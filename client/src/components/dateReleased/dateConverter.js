const dateConverter = (date) => {
  const arrayDate = date.split("-");
  const años = arrayDate[0];
  const mes = arrayDate[1];
  const dia = arrayDate[2];
  //   switch (date) {
  //     case value:

  //     default:
  //         break;
  //   }
};

export default dateConverter;
